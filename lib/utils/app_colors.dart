import 'package:flutter/material.dart';

const colorPrincipal = Color.fromRGBO(26,91,170,1);
const colorFondo = Colors.white;
const colorTexto1 = Colors.black;
const colorError = Colors.red;
const colorGray1 = Color.fromRGBO(80, 80, 80, 1);
const colorExito = Colors.green;
const colorAdvertencia = Colors.orange;
const colorInfo= Color.fromRGBO(60, 60, 60, 1);